apiVersion: v1
kind: Service
metadata:
  name: amdbank-kafka
  labels:
    app.kubernetes.io/name: kafka
spec:
  type: LoadBalancer
  ports:
    - port: 9092
      targetPort: 9092
      protocol: TCP
      name: kafka
  selector:
    app.kubernetes.io/name: kafka