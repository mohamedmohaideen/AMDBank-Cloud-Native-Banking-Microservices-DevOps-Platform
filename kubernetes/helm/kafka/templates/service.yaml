apiVersion: v1
kind: Service
metadata:
  name: amdbank-kafka
  labels:
    app: amdbank-kafka
spec:
  type: ClusterIP
  ports:
    - name: kafka
      port: 9092
      targetPort: 9092
      protocol: TCP
  selector:
    app: amdbank-kafka